<local:ViewBase x:Class="Kanban.View.WPF.TaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kanban.View.WPF"
        mc:Ignorable="d"
        Title="Task Editor" Height="350" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="151*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,-12" Grid.Row="1">
            <TextBlock Text="Название:"/>
            <TextBox Text="{Binding Title}" Margin="0,5,0,10" Height="25"/>

            <TextBlock Text="Описание:"/>
            <TextBox Text="{Binding Description}" Margin="0,5,0,10" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

            <TextBlock Text="Дедлайн:"/>
            <DatePicker SelectedDate="{Binding DeadLine}" Margin="0,5,0,10"/>

            <TextBlock Text="Приоритет:"/>
            <ComboBox ItemsSource="{Binding PrioritiesList}" 
          SelectedValue="{Binding Priority, Mode=TwoWay}"
          SelectedValuePath="Key"
          DisplayMemberPath="Value"
          Margin="0,5,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" IsCancel="True" Width="80" Height="25" Margin="0,0,10,0"/>
                <Button Content="Сохранить" IsDefault="True" Command="{Binding SaveCommand}" Width="80" Height="25"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</local:ViewBase>